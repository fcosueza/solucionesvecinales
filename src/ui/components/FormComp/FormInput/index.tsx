import { InputType } from "@/types";
import { FormFieldAttrs } from "@/types";

const FormInput = () => {
  return (
    <div>
      <label htmlFor=""></label>
      <input type="text" />
    </div>
  );
};

export default FormInput;
