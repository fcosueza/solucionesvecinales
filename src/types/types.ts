/**
 * Tipo NavItemData
 *
 * Tipo que usa el componente NavMenu para generar los enlaces del menu, compuesto
 * por el texto del enlace y la URL.
 */
export interface NavItemData {
  text: string;
  url: string;
}
