/**
 * Modelo Contact
 *
 * Modelo de la tabla de contactos, donde se almacenarán los mensajes creados
 * con el formulario de contacto.
 */

 model Contacto {
    nombre     String
    correo     String  
    mensaje    String
    createdAt  DateTime @default(now())

    @@id([nombre, correo, createdAt])

 }