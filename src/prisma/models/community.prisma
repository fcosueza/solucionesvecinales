model Community {
    id              Int     @id @default(autoincrement())
    name            String
    street          String
    number          Int
    town            String
    province        String
    country         String

    messages       Message[]
    areas          Area[]
    incidents      Incident[]
    registrations  Registration[]
    requests       Request[]
 }